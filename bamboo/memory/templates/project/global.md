# Global Project Memory

Small, high-value project knowledge that should always be available in project mode.

For global project knowledge, keep entries useful across projects. For a specific project, this same template is copied into that project's knowledge directory.

Add entries under `## Notes`. Each entry should include `source: session_id/task_id`.

## Notes
