---
description: Review current changes and draft a concise commit message.
---

Review the current repository changes and draft a concise commit message.

User arguments:
$ARGUMENTS

Use existing read-only git commands through `bash` when needed. Do not create a commit unless the user explicitly asks for that action.
