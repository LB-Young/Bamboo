"""Core module for AutonCode."""


def run_agent(task: str = "") -> None:
    """Run the AutonCode agent.

    Args:
        task: Optional task description to execute.
    """
    # TODO: implement the actual agent run loop
    print(f"AutonCode agent started (task={task!r})")